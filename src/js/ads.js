document.addEventListener("DOMContentLoaded", () => {
    // Логика для управления рекламными баннерами
    // Закомментированный код оставлен для справки
    /*
    const is_specific = document.querySelector('fieldset .field-specific_pages');
    const pagesBlock = document.querySelector('fieldset .field-pages');

    is_specific?.addEventListener('change', (e) => {
        pagesBlock?.classList.toggle('hidden');
    });
    
    if (!is_specific?.checked && pagesBlock) {
        pagesBlock.classList.add('hidden');
    }
    */
});
